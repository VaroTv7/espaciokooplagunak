# Compiled Object files
*.slo
*.lo
*.o
*.obj

# Compiled Dynamic libraries
*.so
*.dylib
*.dll

# Compiled Static libraries
*.lai
*.la
*.a
*.lib

# Executables
*.exe
*.out
*.app

# Code::Blocks runtime files
*.depend
*.layout

# OS temp files
thumbs.db
.DS_Store
Desktop.ini
*~

# output directory
.bin

# vim
*.swp
*.swo

# IDEs (keep shared project settings only when explicitly agreed)
.idea/

# gettext temp files
*.po~

options.ini
keybindings.json
.env
.env.*
!.env.example


.vscode
logs
*.log
build*/
_build*/
CMakeUserPresets.json
compile_commands.json
cmake/_win32/
cmake/android/
script_docs/__pycache__/

# Python
__pycache__/
*.pyc
bridge/.venv/

# Artefactos locales de distribución
/dist/

# La configuración de Sonar pasó a ser del repositorio, no local: la usa el
# workflow `sonarcloud.yml`, y las exclusiones (qué es nuestro y qué es de
# upstream) son una decisión compartida, no una preferencia de cada máquina.
.evidencia-tinte/

# Worktrees de tareas de agentes automáticos (ver SECURITY.md)
.worktrees/

# Residuo de las herramientas de auditoria, no es del repositorio
.datos-auditoria/

# Cache de los clientes de API. Se colo en el PR #658 por un `git add -A`:
# `artic.py` escribia sus ficheros junto al propio modulo, dentro del arbol.
# La ruta ya esta corregida —van al directorio temporal— pero la red queda
# puesta, porque este fallo no lo detecta ningun test.
tools/.artic_search*.json
tools/.nasa3d_meta.json
lagunak-artic-*.json
lagunak-apis.sqlite

# Artefactos de cobertura de node/nyc: los deja cualquier pasada de
# `--experimental-test-coverage` o de nyc en el arbol, y no son entregables.
# El 2026-08-22 un `git add -A` estuvo a punto de meter 17 ficheros de estos
# en un PR de seguridad.
.nyc_output/
coverage/

# Artefactos de cobertura de pytest-cov.
.coverage
*.coverage
htmlcov/

# Y lo que deja el `npm install` que instala ese nyc. El arbol no es un proyecto
# npm —el modulo se prueba con `node --test` a secas, sin dependencias— asi que
# estos tres ficheros son siempre restos de una herramienta de paso, nunca algo
# que se entregue.
#
# El 2026-08-22 aparecieron los tres en el checkout principal a media sesion. Es
# la misma historia que la de arriba con `.nyc_output/`, y la razon de anotarla
# es que la de arriba NO los cubria: ignorar la salida de una herramienta y no
# lo que esa herramienta necesita para correr deja la mitad del rastro fuera.
node_modules/
package.json
package-lock.json

# Excepción deliberada: `tools/e2e-visual/` SÍ es un proyecto npm de verdad
# (Playwright para grabar demos de QA visual sobre Foundry real, #458) y sus
# `package.json`/`package-lock.json` son el propio entregable, no restos de
# paso — por eso viven fuera de `foundry-module/`, que sigue sin declarar
# ninguna dependencia. Su `node_modules/` sigue ignorado por la regla de arriba.
!tools/e2e-visual/package.json
!tools/e2e-visual/package-lock.json
